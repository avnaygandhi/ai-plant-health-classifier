services:
  plant-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: plant_ai_app
    ports:
      - "8501:8501"   # Streamlit UI
      - "8000:8000"   # FastAPI Backend
    restart: unless-stopped